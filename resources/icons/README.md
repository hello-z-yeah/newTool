Place PNG/SVG navigation icons here.  Every `StateButton` is a native
`QPushButton`, so icons can be assigned with `button.setIcon(QIcon(path))` and
`button.setIconSize(QSize(16, 16))` without changing the button state logic.
